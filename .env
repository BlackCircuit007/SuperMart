# ==================== Backend (Node.js/Express) ====================
# Used for /config endpoint (serves EmailJS config to frontend)
# Render.com sets PORT automatically
PORT=3000

# ==================== EmailJS (primary email sending) ====================
# These values are fetched by the frontend via the /config endpoint
# and used to initialize the EmailJS SDK for register/login flows.
# Get these from your EmailJS dashboard (https://dashboard.emailjs.com).
EMAILJS_SERVICE_ID=your_service_id_here
EMAILJS_TEMPLATE_ID=your_template_id_here
EMAILJS_LOGIN_TEMPLATE_ID=your_login_template_id_here
EMAILJS_PUBLIC_KEY=your_public_key_here

# ==================== Legacy SMTP (fallback only) ====================
# The Node.js backend can use these as a fallback if EmailJS fails.
# Not required if EmailJS is configured.
EMAIL_APP_PASSWORD=pkum ickf gtfx wstu
EMAIL_ADDRESS=goodluckiyke2010@gmail.com

# ==================== Environment ====================
NODE_ENV=production
